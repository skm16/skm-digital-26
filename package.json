{
  "name": "skm-digital",
  "version": "1.0.0",
  "private": true,
  "description": "SKM.digital marketing site - building cool sh*t for people doing good work",
  "scripts": {
    "start": "serve . --listen tcp://0.0.0.0:${PORT:-3000} --no-clipboard --no-port-switching",
    "dev": "serve . --listen tcp://0.0.0.0:3000 --no-clipboard",
    "images": "node scripts/optimize-images.mjs",
    "meta": "node scripts/generate-meta-assets.mjs"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
